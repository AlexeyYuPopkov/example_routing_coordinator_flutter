import 'package:auto_route/auto_route.dart';

/// The two bottom bar tabs.
///
/// An empty shell is a nested router outlet and nothing else, so neither of
/// them needs a widget of its own.
const feedTab = EmptyShellRoute('FeedTab');
const contactsTab = EmptyShellRoute('ContactsTab');
